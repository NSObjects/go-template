package configs

import (
	"testing"
)

func TestNewCfg(t *testing.T) {
	// 这里可以添加配置测试
	// 由于需要配置文件，这里只是示例
	t.Log("config test placeholder")
}

func TestBootstrap(t *testing.T) {
	// 这里可以添加bootstrap测试
	// 由于需要配置文件，这里只是示例
	t.Log("bootstrap test placeholder")
}

func TestStore(t *testing.T) {
	cfg := Config{
		System: SystemConfig{
			Port: ":8080",
		},
	}

	store := NewStore(cfg)
	current := store.Current()

	if current.System.Port != ":8080" {
		t.Errorf("expected port :8080, got %s", current.System.Port)
	}
}
